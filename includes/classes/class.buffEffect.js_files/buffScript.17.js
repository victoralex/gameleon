	
	//
	// Blood Drinking
	//
	
	var log = require( "../class.log" );
	
	exports.script = function( buffObject )
	{
		/*
		this.specific = function()
		{
			//setInterval( function() { buffObject.source.modXP( 15 ); }, 1000 );
			
			var myCharacter = buffObject.getCharacter();
			
			if( myCharacter.properties.character_is_alive != null )
			{
				myCharacter.die({ killerCharacterObject: myCharacter });	// suicide
				
				setTimeout( function()
				{
					//myCharacter.resurrect({ resurrectCharacterObject: myCharacter });	// too powerful
				}, 4000 );
			}
		}
		*/
	}