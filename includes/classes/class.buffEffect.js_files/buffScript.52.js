	
	//
	// Agonizing Blow Secondary Damage	
	//
	
	var log = require( "../class.log" );
	
	exports.script = function( buffObject )
	{
		this.specific = function()
		{
			//buffObject.getSource().getInstance().sendBattlegroundStatisticsToAllPlayers();
		}
	}